import SwiftUI

struct SynestheticExperiencesWrapper: UIViewControllerRepresentable {
    func makeUIViewController(context: Context) -> SynestheticExperiencesView {
        return SynestheticExperiencesView()
    }

    func updateUIViewController(_ uiViewController: SynestheticExperiencesView, context: Context) {
       
    }
}
